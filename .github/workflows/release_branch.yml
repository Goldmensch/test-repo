name: Creates an release branch on release

on:
  release:
    types: [published]

jobs:
  create_brach:
    runs-on: ubuntu-latest
    steps:
      - name: get version
        run: echo "$GITHUB_REF_NAME" | grep "([0-9]+)" -P -o | head -1 >> $GITHUB_STATE
      - run: echo "${{env.action_state}}